<li id="diary-<%= diary.id %>">
  
  <span class="content">
    <%= diary.content %>
    
  </span>
  <span class="timestamp">
    Posted <%= time_ago_in_words(st.created_at) %> ago.
  
  <% if current_user?(micropost.user) %>
      <%= link_to "delete", micropost, method: :delete,
                                       data: { confirm: "You sure?" } %>
    <% end %>
  </span>
</li>